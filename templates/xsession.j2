#!/bin/bash

urxvtd -f
redshift &

{{ base_scripts_dir }}/screen.sh left

feh --bg-fill --randomize wallpaper/current/

{{ base_scripts_dir }}/keyboardlayout.sh

{% if x_enable_autolock %}
{{ base_scripts_dir }}/autolock.sh enable
{% endif %}

dbus-update-activation-environment --systemd DISPLAY
eval $(gnome-keyring-daemon --start --components=ssh,gpg)
export GPG_AGENT_INFO SSH_AUTH_SOCK

xsetroot -cursor_name left_ptr
compton -b

exec /usr/bin/xmonad
